import{_ as t,o as a,c as o,an as i}from"./chunks/framework.finZBWsk.js";const m=JSON.parse('{"title":"Pitfalls and gotchas","description":"","frontmatter":{},"headers":[],"relativePath":"pitfalls.md","filePath":"pitfalls.md","lastUpdated":null}'),s={name:"pitfalls.md"};function l(c,e,r,n,d,p){return a(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="Pitfalls-and-gotchas" tabindex="-1">Pitfalls and gotchas <a class="header-anchor" href="#Pitfalls-and-gotchas" aria-label="Permalink to &quot;Pitfalls and gotchas {#Pitfalls-and-gotchas}&quot;">​</a></h1><ul><li><p><code>AccountFunding.FullyFunded</code> forces full-notional margin (no leverage), disallows short exposure, and uses liquidation marks for margin checks so bid/ask spreads do not create synthetic deficits.</p></li><li><p>For <code>MarginRequirement.PercentNotional</code>, margin rates are equity fractions (IMR/MMR style), not collateral-inclusive ratios: configure a &quot;150% short collateral&quot; rule as <code>0.50</code>, not <code>1.50</code>.</p></li><li><p>Principal-exchange spot short-sale proceeds are not automatically lend-eligible: <code>accrue_interest!</code> applies broker-defined short-proceeds rules (<code>broker_short_proceeds_rates</code>) to exclude proceeds from lend base and optionally apply a separate rebate.</p></li><li><p>Use <code>update_marks!</code> to keep equity and margin in sync with prices.</p></li><li><p>Expiry/liquidation helpers use stored side-aware quotes (<code>last_bid</code>/<code>last_ask</code>); keep marks updated with <code>update_marks!</code>.</p></li><li><p>Listed options are cash-settled and assignment-free in Fastback. SPY-like or AAPL-like examples are synthetic proxies, not OCC/IBKR physical-delivery simulations. See <a href="./options_limitations">Options limitations / IBKR mapping</a>.</p></li><li><p>Multi-currency equity depends on <code>ExchangeRates</code> being updated. Once exposure is open, submit FX through <code>process_step!</code>; direct account rate mutation is rejected so valuation caches cannot silently go stale.</p></li><li><p>FX updates must be representable in both directions as finite <code>Float64</code> values; extremely small rates whose reciprocal overflows are rejected without changing the matrix.</p></li><li><p>Register non-base currencies via <code>register_cash_asset!(acc, CashSpec(:EUR))</code>.</p></li><li><p>For variation-margin instruments, fills immediately settle to the current mark basis: execution-to-mark (<code>mark - fill</code>) hits cash on the fill, and post-fill <code>avg_settle_price</code> is the mark. Settled P&amp;L remains attributed to open exposure until reduction/expiry, so <code>fill_pnl_settle</code> (gross realized attribution) can differ from <code>cash_delta_settle</code> (same-fill net cash movement).</p></li><li><p>All stateful account operations enforce non-decreasing timestamps. Equal timestamps are allowed; backdated fills, marks, financing, and lifecycle operations are rejected.</p></li><li><p>Ordinary fills stage their accounting plan before commit, so validation, missing FX, and risk rejection do not require an account snapshot.</p></li><li><p><code>process_step!</code> and multi-stage lifecycle operations are fail-stop: an error leaves completed changes in place and sets <code>acc.poisoned</code>. Do not recover or continue that account; start from a fresh account or replay from your own checkpoint.</p></li><li><p><code>StepSchedule</code> requires at least one unique timestamp. Inputs may be unsorted; the constructor normalizes them chronologically.</p></li><li><p>Corporate actions expect raw, unadjusted quotes and have no replay protection. Accrue financing first, apply each action once, then submit the raw ex-action mark at the same timestamp with repeated accrual disabled.</p></li><li><p><code>OrderRejectError</code> rejection semantics are mainly for <code>fill_order!</code>; expiry/liquidation helpers use internal close-only settlement/liquidation paths instead of exposing normal order lifecycle bypass flags.</p></li><li><p>The poisoned flag reports an aborted mutation, not economic bankruptcy; forced closes can still leave negative balances/equity in stressed scenarios.</p></li><li><p><code>Fastback.plot_*</code> uses built-in SVG by default. For Plots.jl output, load <code>Plots</code> and call <code>set_plot_backend!(:plots)</code>; loading Plots alone does not change the backend. Match the selected backend to the target of <code>!</code> calls (IO for SVG, <code>Plots.Plot</code> for Plots).</p></li></ul>',2)])])}const h=t(s,[["render",l]]);export{m as __pageData,h as default};
